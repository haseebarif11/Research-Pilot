# Core agent framework
langgraph>=0.2.0

# Embeddings via ONNX Runtime — no torch, no Rust-compiled tokenizers
# fastembed has prebuilt wheels for all Python versions including 3.14
fastembed>=0.3.6

# Vector store — replaced chromadb with numpy-based store (see ingestion/vector_store.py)
# chromadb 0.5.x requires tokenizers<=0.20.3 which has no Python 3.14 wheel
# and its Rust build fails (PyO3 max is Python 3.13). numpy is provided by streamlit/pandas.
numpy>=1.22.5

# Web search
duckduckgo-search>=6.0.0

# Frontend
streamlit>=1.35.0

# Backend API
fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.7.0

# Document parsing
pypdf>=4.2.0
python-docx>=1.1.0

# Utilities
tabulate>=0.9.0
requests>=2.31.0
huggingface_hub>=0.23.0
python-dotenv>=1.0.0
