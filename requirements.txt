# CPU-only PyTorch wheels (avoids ~2 GB GPU download on Streamlit Cloud)
--extra-index-url https://download.pytorch.org/whl/cpu

# Core agent framework
langgraph>=0.2.0

# Embeddings — CPU-only torch (~190 MB vs ~2 GB GPU)
torch==2.2.2+cpu
sentence-transformers>=2.7.0,<4.0.0

# Vector database
# chromadb 0.5+ uses onnxruntime (prebuilt wheels for all platforms, no C++ compilation needed)
chromadb>=0.5.0,<0.6.0

# Web search
duckduckgo-search>=6.0.0

# Frontend
streamlit>=1.35.0

# Backend API
fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.7.0

# Document parsing
pypdf>=4.2.0
python-docx>=1.1.0

# Utilities
tabulate>=0.9.0
requests>=2.31.0
huggingface_hub>=0.23.0
python-dotenv>=1.0.0
