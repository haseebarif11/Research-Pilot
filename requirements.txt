# CPU-only PyTorch wheels (avoids ~2 GB GPU download on Streamlit Cloud)
--extra-index-url https://download.pytorch.org/whl/cpu

# Core agent framework
langgraph>=0.2.0

# Embeddings — CPU-only torch keeps install under Streamlit Cloud limits
sentence-transformers>=2.7.0,<4.0.0
torch==2.2.2+cpu
torchvision==0.17.2+cpu

# Vector database
# chromadb 0.5+ requires onnxruntime which fails to build on Streamlit Cloud;
# pin to last pre-onnxruntime release.
chromadb>=0.4.24,<0.5.0

# Web search
duckduckgo-search>=6.0.0

# Frontend
streamlit>=1.35.0

# Backend API
fastapi>=0.111.0
uvicorn>=0.30.0
pydantic>=2.7.0

# Document parsing
pypdf>=4.2.0
python-docx>=1.1.0

# Utilities
tabulate>=0.9.0
requests>=2.31.0
huggingface_hub>=0.23.0
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0
